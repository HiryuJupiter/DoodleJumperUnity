using UnityEngine;

namespace AIAssignment3
{
    public class WarpAroundBehavior : MonoBehaviour
    {

        //private void WrapAround()
        //{
        //    if (Position.x < -14) Position = new Vector2(14, Position.y);
        //    if (Position.y < -8) Position = new Vector2(Position.x, 8);
        //    if (Position.x > 14) Position = new Vector2(-14, Position.y);
        //    if (Position.y > 8) Position = new Vector2(Position.x, -8);
        //}
    }
}